machine:
  php:
    version: 7.0.11
compile:
  override:
    - echo "Compiling.."
dependencies:
  pre:
    - wget https://releases.hashicorp.com/terraform/0.9.2/terraform_0.9.2_linux_amd64.zip
    - unzip terraform_0.9.2_linux_amd64.zip
    - ./terraform --version
    - npm install -g dredd@3.3.1 lambda-local@1.4.2
test:
  override:
    - git clone git@github.com:artrunde/terraform-infrastructure.git
    - ./terraform output -state=terraform-infrastructure/dev/services/osman/terraform.tfstate
    - ./terraform output -state=terraform-infrastructure/dev/services/rodin/v1/terraform.tfstate active
    - npm test
deployment:
  v1:
    tag: '/v[1]+(\.[0-9]+)*/'
    owner: artrunde
    commands:
      - ./deploy.bash $CIRCLE_SHA1 prd
  development:
    branch: development
    commands:
      - ./deploy.bash $CIRCLE_SHA1 dev